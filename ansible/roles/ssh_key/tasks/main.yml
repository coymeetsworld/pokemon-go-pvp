---

- name: Create SSH Key to Droplet
  digitalocean.cloud.ssh_key:
    state: present
    token: "{{ digitalocean_token }}"
    public_key: "{{ public_key }}"
    name: "{{ ansible_env['USER'] }}-key"
  register: ssh_key

...